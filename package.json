{
  "name": "jev-chess-game",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env --import tsx chess-game.ts",
    "check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0",
    "js-chess-engine": "1.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
